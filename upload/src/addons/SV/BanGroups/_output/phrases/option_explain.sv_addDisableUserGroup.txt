When a user is disabled, they can be added to a specific user group. This allows overrides to their user group styling, for example.<br/>
<br/>
This option may be re-applied when "Rebuild user caches" is run, but respects if an admin has removed the user-group manually.